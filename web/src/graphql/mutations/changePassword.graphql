mutation ChangePassword(
  $token: String!
  $newPassword: String!
  $newPasswordCopy: String!
) {
  changePassword(
    token: $token
    newPassword: $newPassword
    newPasswordCopy: $newPasswordCopy
  ) {
    ...RegularUserResponse
  }
}
